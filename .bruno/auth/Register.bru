meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{API_URL}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "adam.brutsaert@epitech.eu",
    "password": "password123",
    "firstName": "Adam",
    "lastName": "Brutsaert"
  }
}

script:post-response {
  if (res.status === 201) {
    bru.setVar("token", res.body.token);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
